numpy>=1.21
pandas>=1.3
matplotlib>=3.4
shapely>=1.8
ujson>=5.0

# traci's version must match your installed SUMO version. Either pip install
# traci==<your SUMO version>, or skip this line and rely on the traci that
# ships inside %SUMO_HOME%/tools (see README Requirements section).
traci
